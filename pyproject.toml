[project]
name = "hwpx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyhwpx",
    "ipykernel",
    "pyautogui>=0.9.54",
    "hml-equation-parser>=1.0.12",
]
